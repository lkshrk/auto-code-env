#!/bin/sh
set -eu

run_dir=${AUTO_CODE_RUN_DIR:-/run/auto-code-env}
test -s "$run_dir/ready"
test -d "$HOME" -a -w "$HOME"

if [ "${AUTO_CODE_HEALTH_SSHD:-1}" = 1 ]; then
  ssh-keyscan -T 2 -p "${AUTO_CODE_SSH_PORT:-22}" 127.0.0.1 >/dev/null 2>&1
fi
